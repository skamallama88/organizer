[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "organizer"
version = "0.1.0"
description = "Safe file organization daemon"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "jinja2>=3.1,<4",
    "pydantic>=2.9,<3",
    "python-multipart>=0.0.20,<1",
    "py7zr>=0.22,<1",
    "pyyaml>=6,<7",
    "rarfile>=4.2,<5",
    "typer>=0.12,<1",
    "uvicorn>=0.30,<1",
    "watchdog>=6,<7",
]

[project.scripts]
organizer = "organizer.cli:app"
organizer-web = "organizer.webapp:run"

[tool.hatch.build.targets.wheel]
packages = ["src/organizer"]

[tool.hatch.build]
include = ["src/organizer/web_ui/**"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]

[dependency-groups]
dev = [
    "httpx>=0.27,<1",
    "mypy>=1.13,<2",
    "pytest>=8.3,<9",
    "ruff>=0.8,<1",
    "types-pyyaml>=6.0,<7",
]
